#!/bin/bash

read COMMAND_JSON;
echo "$COMMAND_JSON"
#PROMPT=` echo $COMMAND_JSON | jq -r -c '.properties.prompt' `
#COMMAND=` echo $COMMAND_JSON | jq -r -c '.command' | base64 --decode `

#echo "echo $PROMPT"
#echo "$COMMAND"
